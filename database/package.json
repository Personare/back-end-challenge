{
  "name": "database",
  "version": "1.0.0",
  "description": "Banco de dados JsonServer",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "json-server --watch db.json --port 3001"
  },
  "author": "Lucas Alves Marcello",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.16.3"
  }
}
