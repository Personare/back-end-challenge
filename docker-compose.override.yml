version: '3'
services:
  api:
    build:
      context: .
    volumes:
      - ./:/app

  composer:
    image: exchange-composer:0.1.0
    build:
      context: ./composer
    command: composer install
    environment:
      - PHAN_ALLOW_XDEBUG=0
      - PHAN_DISABLE_XDEBUG_WARN=1
    volumes:
      - ./:/app
